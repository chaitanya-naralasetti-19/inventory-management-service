spring:
  application:
    name: Inventory
  datasource:
    url: jdbc:postgresql://localhost:5432/inventory_management
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080